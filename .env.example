# PDF Multimodal RAG — Environment Variables
#
# LOCAL DEVELOPMENT: fill in AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY
# EC2 PRODUCTION:    leave those two blank — the IAM role provides credentials automatically

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=               # leave blank on EC2 (IAM role used instead)
AWS_SECRET_ACCESS_KEY=           # leave blank on EC2 (IAM role used instead)

# AWS Bedrock model IDs
BEDROCK_EMBEDDING_MODEL_ID=amazon.titan-embed-text-v2:0
BEDROCK_LLM_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0

# Application paths (relative to project root)
UPLOAD_DIR=data/uploads
PROCESSED_DIR=data/processed
CHROMA_DB_DIR=data/chroma_db

# Chunking
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
MIN_CHUNK_SIZE=100

# Logging
LOG_LEVEL=INFO
